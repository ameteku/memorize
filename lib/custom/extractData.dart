import 'dart:convert';
//import 'dart:ffi';
import 'dart:typed_data';

import 'package:file_picker/file_picker.dart';
import 'package:csv/csv.dart';

void extractMemories(FilePickerResult file) {
  // List csvFileContentList;
  // String filter = 'key,value';
  // List csvList;
  // var asUint = file.files.first?.bytes;
  //
  // if (asUint != null) {
  //   csvFileContentList = utf8.decode(asUint!.toList()).split('\n');
  //   print("Getting csv contents: ${csvFileContentList.toString()}");
  // }
}
